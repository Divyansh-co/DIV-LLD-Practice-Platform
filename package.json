{
  "name": "div-lld-practice-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && node -e \"const fs=require('fs'); fs.cpSync('dist', '../backend/app/static', {recursive:true});\""
  }
}
